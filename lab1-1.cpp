#include<iostream>
using namespace std;

int main(){
    /* Constants - expressions with fixed value
        Literals: particular values in source code, can be ints, float, char, str, bool
        Defined constants(#define): user defined constants that don't take up memory consuming variables
            #define replaces every subsequent occurrance of the identifier in the source code
            eg: #define NEWLINE '\n' stands in for the latter
            
    return 0;
}
#include<iostream>

using namespace std;

int main(){
    int x;                          // declaration of static type
    x = 4 + 2;                      // initialisation of x, can be done implicitly with 'int x = 4 + 2;'    
    cout << x / 3 << ' ' << x * 2;  // << prints sequence of values
    return 0;                       // use cin for grabbing user input instead
    }